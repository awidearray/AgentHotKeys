[variables]
NODE_ENV = "production"
NIXPACKS_NO_CACHE = "1"

[phases.install]
cmds = ["npm ci --prefer-offline"]

[phases.build]
cmds = ["npm run build"]
cacheDirectories = []

[phases.start]
cmd = "npm start"

providers = ["node"]

[node]
version = "22"